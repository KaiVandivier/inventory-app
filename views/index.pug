extends layout

block content
  h1= title
  p Welcome to #{title}!  This is a simple website to show off some basic features of an Express app.
  p Browse food items in the following categories:

  dl
    each category in categories
      dt: a(href=category.url) !{category.name}
      dd !{category.description}
    
    else
      p (No categories yet.)
